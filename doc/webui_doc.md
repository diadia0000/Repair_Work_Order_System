# Web UI 文件

## 專案概述

**名稱**: Lab Service Portal  
**技術**: React 19 + TypeScript + Vite + Tailwind CSS  
**功能**: 實驗室維修工單管理系統

---

## 系統架構

```
App (主應用)
├── LoginPage (登入頁)
├── RegisterPage (註冊頁)
├── NewPasswordPage (重設密碼)
└── Dashboard (主控台)
    ├── Navigation (導覽列)
    ├── Statistics (統計卡片)
    ├── FilterBar (篩選列)
    ├── TicketGrid (工單列表)
    │   └── TicketCard (工單卡片)
    ├── CreateTicketModal (建立工單彈窗)
    └── TicketDetailModal (工單詳情彈窗)
```

---

## 主要頁面

### 1. 登入頁面 (LoginPage)

**畫面元素**:
- Logo 與應用名稱
- Email 輸入框
- 密碼輸入框 (可切換顯示/隱藏)
- 登入按鈕
- 註冊連結

**功能**:
- 表單驗證 (Email 格式、必填檢查)
- Enter 鍵送出
- 整合 AWS Cognito 認證

---

### 2. 註冊頁面 (RegisterPage)

**畫面元素**:
- 姓名輸入框
- Email 輸入框
- 密碼輸入框
- 註冊按鈕
- 返回登入連結

**密碼規則**:
- 至少 8 字元
- 需包含大小寫字母
- 需包含數字
- 需包含特殊符號

---

### 3. 主控台 (Dashboard)

#### Navigation Bar
```
[Logo] Lab Service Portal    [+ New Ticket]    [User▼]
```
固定於頂部，包含新增工單按鈕與使用者選單

#### 統計卡片
```
┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
│  Total  │ │  Open   │ │Processing│ │ Closed  │
│   12    │ │   5     │ │    4     │ │   3     │
└─────────┘ └─────────┘ └─────────┘ └─────────┘
```
- Total: 總工單數
- Open: 待處理 (黃色標示)
- Processing: 處理中 (藍色標示)
- Closed: 已完成 (綠色標示)

#### 工單列表
**網格布局**:
- 手機: 1 欄
- 平板: 2 欄
- 桌面: 3 欄

**篩選功能**:
下拉選單可選擇 All / Open / Processing / Closed

---

### 4. 工單卡片 (TicketCard)

```
┌───────────────────────────────┐
│ 標題                 [狀態標籤] │
├───────────────────────────────┤
│ 描述內容...                   │
├───────────────────────────────┤
│ 🕐 2025-12-06    ⚠️ High     │
│ 👤 王小明                     │
└───────────────────────────────┘
```

**狀態顏色**:
- Open: 黃色
- Processing: 藍色
- Closed: 綠色

**優先級顏色**:
- Low: 灰色
- Medium: 橙色
- High: 紅色

**互動**:
- Hover 時向上浮動 + 陰影增強
- 點擊開啟詳情頁

---

### 5. 建立工單彈窗 (CreateTicketModal)

**表單欄位**:
1. **Issue Title** (必填): 工單標題
2. **Description** (必填): 詳細說明 (6 行文字框)
3. **Priority** (必填): Low / Medium / High
4. **Images** (選填): 拖放或選擇上傳圖片 (最多 5 張)

**圖片上傳**:
- 支援拖放 (Drag & Drop)
- 點擊選擇檔案
- 即時預覽縮圖
- 可刪除已選擇的圖片

**按鈕**:
- Cancel: 取消並關閉彈窗
- Submit: 建立工單

---

### 6. 工單詳情彈窗 (TicketDetailModal)

**顯示內容**:
- 工單標題與狀態
- 建立者資訊
- 建立時間
- 優先級
- 完整描述
- 上傳的圖片 (可點擊放大)

**管理功能** (Admin only):
- 更新狀態: Open → Processing → Closed
- 刪除工單

**權限控制**:
- 一般使用者: 可查看、刪除自己的工單
- Admin: 可更新狀態、刪除所有工單

---

## 色彩設計

### 主色調
```css
Primary Blue:    #2563EB
Primary Hover:   #1D4ED8
Light Blue:      #DBEAFE
```

### 狀態色
```css
Success (綠):    #10B981
Warning (黃):    #F59E0B
Danger (紅):     #EF4444
```

### 背景與邊框
```css
Background:      #F8FAFC
Card:            #FFFFFF
Border:          #E2E8F0
```

---

## 響應式設計

### 斷點
```
sm:  640px   (手機橫向)
md:  768px   (平板)
lg:  1024px  (桌面)
xl:  1280px  (大螢幕)
```

### 適配策略
- **< 640px**: 單欄布局，隱藏次要資訊
- **640-1024px**: 雙欄布局，顯示完整內容
- **> 1024px**: 三欄布局，最佳閱讀體驗

---

## 狀態管理

### App.tsx 主要狀態
```typescript
isLoggedIn: boolean          // 登入狀態
userEmail: string            // 使用者 Email
userName: string             // 使用者姓名
isAdmin: boolean             // 管理員權限
tickets: Ticket[]            // 工單列表
isLoading: boolean           // 載入狀態
error: string | null         // 錯誤訊息
```

### Ticket 資料結構
```typescript
interface Ticket {
  ticket_id: string;
  title: string;
  description: string;
  status: 'Open' | 'Processing' | 'Closed';
  priority: 'Low' | 'Medium' | 'High';
  created_at: string;
  user_email: string;
  user_name: string;
  images?: string[];
}
```

---

## API 整合

### 認證流程
1. 使用者登入 → AWS Cognito 認證
2. 取得 JWT Token
3. 每次 API 請求帶 Token: `Authorization: Bearer {token}`
4. Token 包含使用者資訊與權限

### 主要 API 操作
```typescript
// 查詢工單
ticketService.getAllTickets()

// 建立工單
ticketService.createTicket({
  title, description, priority,
  user_email, user_name, images
})

// 更新狀態 (Admin)
ticketService.updateTicket(ticketId, status)

// 刪除工單
ticketService.deleteTicket(ticketId)

// 圖片上傳
ticketService.getUploadUrl(fileName, fileType)
ticketService.uploadToS3(uploadUrl, file)
```

---

## 圖片上傳流程

1. 使用者選擇圖片
2. 呼叫 API 取得 S3 Pre-signed URL
3. 直接上傳到 S3
4. 取得圖片 URL
5. 建立工單時附上圖片 URL

**優點**: 不經過 Lambda，節省流量與處理時間

---

## 權限設計

### User (一般使用者)
- 查看所有工單
- 建立新工單
- 刪除自己的工單

### Admin (管理員)
- User 所有權限
- 更新任何工單狀態
- 刪除任何工單

**判斷方式**: 從 JWT Token 的 `cognito:groups` 判斷

---

## 錯誤處理

### 顯示方式
右上角彈出紅色提示框，顯示錯誤訊息，可手動關閉

### 常見錯誤
- 網路錯誤: 顯示「網路連線失敗」
- 認證失敗: 自動導回登入頁
- 權限不足: 顯示「無權限執行此操作」
- API 錯誤: 顯示後端回傳的錯誤訊息

---

## 載入狀態

### 全域載入
顯示轉圈圈動畫與「Loading tickets...」文字

### 按鈕載入
按鈕變灰並顯示 Loading 狀態，防止重複點擊

---

## UI 元件庫

### 使用套件
- **Radix UI**: 無樣式的可訪問性元件
- **Lucide React**: 現代化圖示庫
- **Tailwind CSS**: Utility-first CSS 框架

### 主要圖示
```
Plus:         新增按鈕
LogOut:       登出
User:         使用者
Filter:       篩選
Clock:        時間
AlertCircle:  警告
Eye/EyeOff:   顯示/隱藏密碼
X:            關閉
Upload:       上傳
Trash2:       刪除
```

---

## 開發配置

### 專案結構
```
src/
├── App.tsx                    主應用
├── components/
│   ├── LoginPage.tsx          登入頁
│   ├── RegisterPage.tsx       註冊頁
│   ├── Dashboard.tsx          主控台
│   ├── TicketCard.tsx         工單卡片
│   ├── CreateTicketModal.tsx  建立彈窗
│   └── TicketDetailModal.tsx  詳情彈窗
├── services/
│   └── ticketService.ts       API 服務
├── config/
│   ├── api.ts                 API 配置
│   └── aws-config.ts          AWS Cognito 配置
└── styles/
    └── globals.css            全域樣式
```

### 環境變數
在 `src/config/aws-config.ts` 設定:
```typescript
userPoolId: 'us-east-1_...'
userPoolClientId: '...'
region: 'us-east-1'
```

在 `src/config/api.ts` 設定:
```typescript
API_BASE_URL: 'https://...execute-api.../dev'
```

---

## 執行專案

### 開發環境
```bash
npm install           # 安裝相依套件
npm run dev           # 啟動開發伺服器
```

### 建置部署
```bash
npm run build         # 建置生產版本
npm run preview       # 預覽建置結果
```

---

## 效能優化

### 已實作
- React 19 自動批次更新
- Vite 快速熱重載
- Tailwind JIT 編譯
- Code Splitting

### 可改進
- 工單列表虛擬滾動 (資料量大時)
- 圖片 Lazy Loading
- Service Worker 離線快取
- React.memo 減少不必要渲染

---

## 使用者體驗

### 互動回饋
- 按鈕 Hover: 顏色變深 + 陰影
- 卡片 Hover: 向上浮動
- 表單驗證: 即時錯誤提示
- 操作成功: 自動重新整理列表

### 鍵盤操作
- Enter: 送出表單
- ESC: 關閉彈窗
- Tab: 焦點導航

---

## 無障礙設計

### 符合標準
- WCAG 2.1 AA 等級
- 鍵盤完全可操作
- 螢幕閱讀器友善
- 顏色對比度足夠

### 實作方式
- 適當的 ARIA 標籤
- Semantic HTML
- 焦點管理
- 明確的按鈕文字
